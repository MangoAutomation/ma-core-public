package com.serotonin.m2m2.module;

/**
 * This class allows a convenient way to perform lifecycle behaviours common to a module. It defines no methods itself,
 * an so is a clean way to perform initialization and termination logic.
 */
public abstract class LifecycleDefinition extends ModuleElementDefinition {
    // no op
}
